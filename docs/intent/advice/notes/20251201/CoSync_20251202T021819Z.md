# CoSync — advice ingest + AIFD cleanup

- Advice: user .md ingested under docs/intent/advice/notes/20251201
- AIFD: workflow ids and CTA text patched
- Verify: search for TOS-AI or TOS AI returned:
.github/workflows/lead-alert.yml:34:          OPU=$(jq '[.results[] | select(.page | contains("TOS-AI_OnePager")) | .visitors // 0] | add // 0' pages.json)
.github/workflows/pages-smoke.yml:19:          if grep -E 'id="tosai-(explainer|checklist|cta)"|Request a TOS-AI' page.html; then
docs/intent/advice/notes/20251201/Advice_CoPolitic_20251202T015457Z.md:7:1) Remove all 'TOS-AI' wording and ids. Prefer 'AIFD' and 'AI Front Door'.

Next:
- Publish a small banner linking to the Academia Shock release tag
- After DOI is minted, add cite line to CoPolitic header